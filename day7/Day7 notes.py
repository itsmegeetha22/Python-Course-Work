DICT OPERATIONS:

MAX
MIN
SORTDE
GET
SETDEFAULT(IT SETS THE DEFAULT VALUE WHEN THERE IS NO NUMBER IN THE DICT)

SET
 ->UNORERED
 ->MUTABLE
 ->UNIQUE VALUES
 ->HETROGENOUS

set operations: union intersection difference symmetric disjoint subset superset
add update pop clear remove discard(if any case of error it doesnot show that error)


CONDITIONAL STATEMENTS:TO MAKE DECISIONS
    ->SIMPLE IF(SINGLE ACTION)SYNTAX:IF(CONDITION):
        #STATEMENTS
    ->IF ELSE(TWO ACTIONS)SYNTAX:IF(COND):
        #STATEMENTS
        ELSE:
            #STATEMENTS
    -> ELSEIF LADDER:MORE THAN ONE CONDITIONS SYNTAX:IF-ELIF...ELIF-ELSE
    IF C1:
        #1 STATEMENT
    ELIF C2:
        #2 STATEMENT


.........
ELIF CN:
    #N STATEMENT
ELSE:
    #FINAL STATEMENT
    -> NESTED IF(CONDITION INSIDE CONDITION)
    ->SYNTAX:IF C1:
               IF C2:
                   
